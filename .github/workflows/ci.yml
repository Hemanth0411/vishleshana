name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install -r requirements-dev.txt

      - name: Lint with ruff
        run: ruff check codelens/

      - name: Format check with black
        run: black --check codelens/

      - name: Run tests
        run: pytest tests/ --cov=codelens --cov-report=term-missing
        env:
          NIM_API_KEY: dummy_key_for_tests

      - name: Check Attribution
        run: |
          pip install pip-licenses
          pip-licenses --format=markdown > ATTRIBUTION_CHECK.md
          # This is a soft check for now to ensure the file exists
          ls ATTRIBUTION.md
